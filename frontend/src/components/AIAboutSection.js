import React from 'react';

// Temporary stub for AIAboutSection to isolate rendering issues.
const AIAboutSection = () => (
	<section style={{padding: 24}}>
		<h2 style={{color: '#fff'}}>About (temporary)</h2>
		<p style={{color: '#bbb'}}>This is a temporary placeholder for AIAboutSection while debugging render issues.</p>
	</section>
);

export default AIAboutSection;
